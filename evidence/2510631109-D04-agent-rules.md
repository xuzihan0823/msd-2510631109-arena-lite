# D04 Agent Rules 会话记录

- 学号：2510631109
- 日期：2026-07-08
- 工具：Hermes Agent
- 模型：gpt-5.5
- 项目路径：`/Users/mac/Desktop/短学期实践/msd-work/msd-2510631109-arena-lite`
- 规则文件：`AGENTS.md`

## 会话目标

要求 Agent 先读取项目根目录中的 `AGENTS.md`，复述安全规则与工程规则，然后只执行只读检查和复测，不修改文件、不联网安装、不提交、不推送。

## Agent 识别到的规则摘要

根据 `AGENTS.md` 与会话截图，Agent 已识别并复述以下边界：

### 安全规则

- 不把真实账号、API Key、Token、支付信息或私有服务地址写入仓库、文字记录、提交信息或报告。
- 未经人工确认，不删除文件，也不改动项目目录之外的文件。
- 未经人工确认，不执行联网购买、订阅、充值、发布、推送或自动提交。
- 如果任务需要新的凭据、在线额度或外部服务配置，需要先停下来说明原因。

### 工程规则

- 修改前先说明准备创建或修改哪些文件。
- 修改后说明实际修改的文件。
- 完成任务后提供测试命令和关键输出。
- 项目优先小步修改、小步测试、小步提交。
- 当前阶段只保留 `/health` 最小骨架，不扩展主要业务功能。

## 本次只做复测

本次会话按规则只做了读取与复测：

- 读取了 `AGENTS.md`
- 读取了 `app/main.py`
- 读取了 `tests/test_health.py`
- 读取了 `pytest.ini`
- 读取了 `requirements.txt`
- 执行了 `pytest -q`

未进行以下高风险动作：

- 未安装新依赖
- 未联网购买或配置新凭据
- 未删除文件
- 未提交、推送或发布
- 未读取项目外敏感文件

## 复测命令

```bash
cd /Users/mac/Desktop/短学期实践/msd-work/msd-2510631109-arena-lite
source .venv/bin/activate
pytest -q
```

## 复测结果

```text
.                                                                        [100%]
1 passed in 0.25s
```

## 会话截图辅证

- `evidence/screenshots/2510631109-D04-agent-rules.png`
- `evidence/screenshots/2510631109-D04-agent-permissions.png`

其中 `2510631109-D04-agent-rules.png` 展示了 Agent 读取规则文件、识别边界并只执行 `pytest` 复测的会话过程。

## 检查结论

- Agent 已读取 `AGENTS.md`。
- Agent 已复述安全规则与工程规则。
- 本次只执行了只读检查和 `pytest -q` 复测。
- `pytest -q` 通过，结果为 `1 passed`。

## 安全说明

本记录不包含真实账号、API Key、Token、支付信息、私有地址或个人敏感信息。
