# D05 Task Cards - 2510631109

本文件由 Hermes Agent 的 `gpt-5.6-terra` 生成第一版，已由 Claude Code 的 `claude-opus-4-8[1m]` 完成只读复核。D05 只准备后续文档、契约和验收边界，不新增主要业务功能。

## Card 1 - 明确 D06 的用户故事、验收标准与非目标

- Role（角色）：`pm-2510631109`
- Context（上下文）：当前 arena-lite 只有已经通过测试的 `/health` 最小骨架；D06 将编写 PRD/SPEC，需要先控制需求范围。
- Task（任务）：为 D06 准备 3 到 5 个用户故事，每个故事至少给出 2 条“操作 -> 预期输出”的验收标准，并明确非目标和至少 2 条错误路径。
- User Value（用户价值）：让后续文档和实现围绕可验收的用户场景，避免从健康检查直接跳到过大的完整系统。
- Acceptance Criteria（验收标准）：`docs/PRD.md` 和 `docs/SPEC.md` 形成后，能定位到 3 到 5 个用户故事、每个故事至少 2 条验收标准、非目标和错误路径；内容与当前 `/health` 最小骨架不冲突。
- Test Command（测试命令）：`test -f docs/PRD.md && test -f docs/SPEC.md && rg -n "用户故事|验收标准|非目标|错误路径" docs/PRD.md docs/SPEC.md`
- Non-goals（非目标）：不实现用户、对战、排行榜、数据库、登录或任何新的业务 API。
- Risk（风险）：故事范围过大或验收标准写成不可验证的形容词；需要由 reviewer 检查每条标准是否可以转成测试。
- Estimated Effort（预计工作量）：60 分钟。

## Card 2 - 设计最小 API 契约与错误路径草案

- Role（角色）：`architect-2510631109`
- Context（上下文）：D03 已证明 FastAPI 服务可启动；后续功能必须先有可解释的输入、输出、状态码和错误路径，不能直接堆业务代码。
- Task（任务）：为 D06/D07 准备最小 API 契约草案，保留现有 `GET /health`，并列出后续业务接口的输入、输出、鉴权边界、成功状态码和至少 2 种错误状态。
- User Value（用户价值）：让开发与 QA 对“接口做到什么程度算完成”达成一致，并尽早暴露错误路径。
- Acceptance Criteria（验收标准）：`docs/SPEC.md` 形成后，至少包含 `/health`、最小业务接口草案、请求字段、响应字段、成功状态码、错误状态码、错误响应和不支持的操作；每个接口都能映射到用户故事或非目标。
- Test Command（测试命令）：`rg -n "API Contract|/health|状态码|错误路径|鉴权" docs/SPEC.md && git diff --check`
- Non-goals（非目标）：不修改 `app/main.py`，不实现业务端点，不引入数据库、认证库或外部服务。
- Risk（风险）：接口数量过多、错误状态遗漏或把实现细节提前写死；需要由 reviewer 检查范围是否仍可在课程周期内完成。
- Estimated Effort（预计工作量）：75 分钟。

## Card 3 - 规划测试、证据与提交边界

- Role（角色）：`qa-2510631109`
- Context（上下文）：D03/D04 已有 `/health`、pytest、Agent 规则和证据；后续 D06-D11 需要持续留下可复现命令和小步提交记录。
- Task（任务）：创建 `process/2510631109-d6-evidence-checklist.md`，在其中列出 D03/D04 已有证据路径、D06 要保存的测试命令和证据路径、非目标、提交边界，以及可供后续任务卡复用的九字段检查清单。
- User Value（用户价值）：评审者能够复现关键链路，团队能区分“文档准备完成”和“业务功能已实现”，避免用大提交掩盖过程。
- Acceptance Criteria（验收标准）：`process/2510631109-d6-evidence-checklist.md` 已创建，且能定位到 D03/D04 证据路径、D06 的测试命令、证据路径、非目标和提交边界；当前 `pytest -q` 通过；提交前 `git diff --check` 无输出。
- Test Command（测试命令）：`test -f process/2510631109-d6-evidence-checklist.md && rg -n "D03|D04|Test Command|Evidence Path|Non-goals|Commit Boundary" process/2510631109-d6-evidence-checklist.md && source .venv/bin/activate && pytest -q && git diff --check`
- Non-goals（非目标）：不新增业务测试、不修改现有 FastAPI 路由、不创建远程仓库或推送。
- Risk（风险）：清单可能只罗列截图而缺少可复现命令，或遗漏失败路径；需要由 reviewer 检查清单内容是否对应实际证据和当前状态。
- Estimated Effort（预计工作量）：45 分钟。
