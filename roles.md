# arena-lite Roles - 2510631109

本文件用于约束 arena-lite 跟练项目中的协作角色。即使当前由一名同学完成，也要用不同角色视角审查工作。

## Roles（角色）

- `pm-2510631109`：澄清需求，维护任务卡，确认每张卡都有可测试验收标准与明确非目标。
- `architect-2510631109`：做设计取舍，维护接口边界、错误路径、非目标和 ADR。
- `dev-2510631109`：只从已接受的任务卡开始小步实现，保持相关测试通过，不擅自扩大范围。
- `qa-2510631109`：设计并执行验收测试，记录主路径、失败路径和命令输出。
- `reviewer-2510631109`：审查敏感信息、任务范围、提交粒度、文字证据和手册符合度。

## Model Assignment（模型分工）

- 具体模型、可发送数据、预算上限、Fallback 和证据路径记录在 `process/model-roster-2510631109.md`。
- D05 的第一版任务卡由 Hermes Agent 的 `gpt-5.6-terra` 生成。
- D05 的独立复核已使用 Claude Code 的 `Opus 4.8 (1M context)` 完成；复核只处理了已脱敏的角色、skill、model roster 和任务卡内容，截图证据见 `evidence/screenshots/2510631109-D05-claude-review.png`。
- 生成与复核必须保留提示词、模型、输出摘要和复核结论；不得把真实账号、API Key、Token、支付信息或私有服务地址放入记录。

## Working Rule（工作规则）

每次让 AI Agent 工作前，先明确角色、模型、允许读取的文件和禁止动作。每次工作结束后，由不同模型承担 `qa-2510631109` 或 `reviewer-2510631109` 复核。若第二模型因认证、额度或网络不可用，应如实记录 blocked 状态和影响，不得伪造“多模型复核已完成”。
